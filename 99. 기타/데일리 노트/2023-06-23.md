# 오늘 배운 것
- 

# 메모
- IntersectionObserver API 사용
	```
	// console.log(item.boundingClientRect); // 타겟 el의 정보 반환
	// console.log(item.rootBounds); //루트 el의 정보 반환
	// console.log(item.intersectionRect); //교차 정보 반영
	// console.log(item.intersectionRatio); // 타겟과의 교차 정보를 0.0 ~ 1.0사이로 반환
	// console.log(item.isIntersecting); // 타겟과의 교차 영역에 있을 경우 true 반환
	// console.log(item.target); // 타겟 el 반환
	// console.log(item.time); // 교차가 기록된 시간 반환
	```
